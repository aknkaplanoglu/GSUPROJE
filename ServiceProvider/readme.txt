Proje Spring Tool Suite(STS) çalıştırılmıştır.
Proje import edildikten sonra test için mvn jetty:run yapılarak test edilebilir.
Proje clean edildikten sonra Servers sekmesinden App Server'a deploy edilir.